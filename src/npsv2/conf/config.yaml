defaults:
  - pileup: five_channel
  - generator: single
  - pileup_generator: ${defaults.1.generator}
    optional: true
  - simulation: augmented
  - model: joint_embeddings
  - training
  - model_training: ${defaults.4.model}
    optional: true

command: genotype
threads: 1

reference: ???
shared_reference: null
stats_path: ???

input: ???
reads: ???
output: ???

sample: null